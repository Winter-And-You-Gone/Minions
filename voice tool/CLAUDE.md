# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Voice tool — a new project in the `X:\Minions\voice tool` directory. This is a greenfield project with no existing code, configuration, or dependencies.

## Getting Started

This section should be updated as the project is scaffolded:

- **Build command**: 
- **Dev server**: 
- **Lint**:
- **Test single file**:
- **Test suite**:

## Architecture

This section should be updated as the project takes shape.

## Conventions

- The `X:\Minions\voice tool` directory (`%MINIONS_ROOT%`) is the project root.
- Platform is Windows (PowerShell).
